<Window x:Class="SkolaProgramiranja.Views.UrediIliDodajInstruktoraView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkolaProgramiranja.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/shared"
        mc:Ignorable="d"
        Title="{DynamicResource WindowTitle_AddOrEditInstructor}" Height="450" Width="400" WindowStartupLocation="CenterScreen" >
    <Window.InputBindings>
        <KeyBinding Key="Enter"
                Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=btnSacuvaj.Command}"
                CommandParameter="{Binding ElementName=btnSacuvaj}" />
    </Window.InputBindings>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource Form_AddOrEditInstructor_Title}"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{DynamicResource MaterialDesignBody}"
                       Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <TextBlock Text="{DynamicResource Hint_FirstName}" Style="{StaticResource MaterialDesignBody1TextBlock}" />
                <TextBox x:Name="txtIme"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,5"
                         MaxHeight="100"/>

                <TextBlock Text="{DynamicResource Hint_LastName}" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,10,0,0"/>
                <TextBox x:Name="txtPrezime" 
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,5"/>

                <TextBlock Text="{DynamicResource Hint_Email}" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,10,0,0"/>
                <TextBox x:Name="txtEmail"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,5"/>

                <TextBlock Text="{DynamicResource Hint_NewPassword}" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,10,0,0"/>
                <!-- LOZINKA + OČI -->
                <Grid >
                    <!-- Sakrivena verzija -->
                    <PasswordBox x:Name="txtLozinka"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        materialDesign:HintAssist.Hint="{DynamicResource Hint_Password}"
                        Visibility="Visible"
                                 />

                    <!-- Prikazana verzija -->
                    <TextBox x:Name="tbLozinka"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        materialDesign:HintAssist.Hint="{DynamicResource Hint_Password}"
                        Visibility="Collapsed" />

                    <Button x:Name="btnShowPassword"
                        Width="28" Height="28"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        Click="btnShowPassword_Click"
                            
                        BorderBrush="Transparent">
                        <materialDesign:PackIcon x:Name="iconEye"
                            Kind="EyeOutline" />
                    </Button>
                </Grid>

                <TextBlock Text="{DynamicResource Hint_Education}" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,10,0,0"/>
                <TextBox x:Name="txtObrazovanje"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,5"/>

                <TextBlock Text="{DynamicResource Hint_Biography}" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,10,0,0"/>
                <TextBox x:Name="txtBiografija"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Height="80"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Margin="0,5"/>
            </StackPanel>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                <Button Content="{DynamicResource Btn_Save}"
                        Click="Sacuvaj_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,10,0"/>
                <Button Content="{DynamicResource Btn_Cancel}"
                        Click="Otkazi_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
